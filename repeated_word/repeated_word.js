'use strict';

let arr = str.toLowerCase().split(' ');
let temp = [];

for (let i=0; i<arr.length; i++) {
  if(temp.length === 0) {
    temp.push(arr[i]) [one]
  } else {
    for(let j=0; j<arr.length; j++) {
      if(arr[i].replace(',') === temp[i].replace(' ')) {
        return arr[i];
      } else {
        temp.push(arr[i] [one,two]})
      }
  }
}